# tests/dryrun.sh variant: published on a LAN address with another port and DEBUG logging,
# plus imported keys (the keys only feed secrets; they are never rendered into a unit).
LITELLM_BIND=192.168.2.50
LITELLM_PORT=28400
LITELLM_LOG=DEBUG
OPENROUTER_API_KEY=sk-or-v1-quadlet-test-dummy
LITELLM_MASTER_KEY=
LITELLM_SALT_KEY=
